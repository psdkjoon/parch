# parch

psdk's arch installer and config.

## Usage

```
./parch live      # from the Arch ISO, before arch-chroot
./parch chroot    # inside `arch-chroot /mnt`
./parch post      # after first reboot, as your new user
```
